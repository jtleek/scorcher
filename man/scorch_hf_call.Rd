% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scorch_huggingface.R
\name{scorch_hf_call}
\alias{scorch_hf_call}
\title{Call a HuggingFace Model via Inference API and Save Config}
\usage{
scorch_hf_call(
  model_id,
  input,
  api_key = Sys.getenv("HF_API_KEY", unset = NA),
  config_path = file.path(tempdir(), paste0(gsub("[^A-Za-z0-9_]", "_", model_id),
    "_config.json"))
)
}
\arguments{
\item{model_id}{HF repo ID string (e.g. "username/modelname").}

\item{input}{A named list representing the inference payload (e.g. list(inputs="text")).}

\item{api_key}{API token for HF (default from Sys.getenv("HF_API_KEY")).}

\item{config_path}{File path to save downloaded config.json.}
}
\value{
A list with elements:
\itemize{
\item inference: parsed inference API response
\item config: parsed config.json or NULL
\item config_path: path where config.json was written
}
}
\description{
Call a HuggingFace Model via Inference API and Save Config
}
\examples{

\dontrun{
out <- scorch_hf_call(

  model_id = "distilbert-base-uncased",
  input = list(inputs = "Hello!")
)

print(out$config)
}

}
