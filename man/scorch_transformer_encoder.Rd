% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scorch_transformer_encoder.R
\name{scorch_transformer_encoder}
\alias{scorch_transformer_encoder}
\title{Add a Transformer Encoder Layer to a Scorch Model}
\usage{
scorch_transformer_encoder(
  scorch_model,
  name,
  inputs,
  embed_dim,
  num_heads,
  dim_feedforward = 2048,
  dropout = 0.1,
  ...
)
}
\arguments{
\item{scorch_model}{A \code{scorch_model} object created by
\code{\link{initiate_scorch}}.}

\item{name}{A unique character string identifying this node in the
model graph. Names wire the computation graph -- other nodes
reference them via their \code{inputs} argument to define
branching, fusion, and skip connections. Names are arbitrary but
appear in error messages and \code{\link{plot_scorch_model}}
output. Common prefixes: \code{"fc"} (linear), \code{"conv"}
(convolution), \code{"act"} (activation). Use number suffixes
for multiples (e.g., \code{"fc1"}, \code{"fc2"}).}

\item{inputs}{Character vector of a single upstream node name.}

\item{embed_dim}{Integer. The d_model dimension (total embedding
size).}

\item{num_heads}{Integer. Number of attention heads.}

\item{dim_feedforward}{Integer. Dimension of the feed-forward
network (default 2048).}

\item{dropout}{Numeric. Dropout rate (default 0.1).}

\item{...}{Additional arguments passed to
\code{torch::nn_transformer_encoder_layer()}.}
}
\value{
The updated \code{scorch_model} with a new row appended to
its \code{graph} tibble.
}
\description{
Adds a \code{torch::nn_transformer_encoder_layer} node to the
Scorch model graph. Implements one layer of the standard
transformer encoder (self-attention + feed-forward).
}
\details{
The \code{embed_dim} must be divisible by \code{num_heads}. This
adds a single encoder layer; stack multiple calls for deeper
transformers.
}
\examples{
\dontrun{
# Two-layer transformer encoder
model <- model |>
  scorch_transformer_encoder("enc1", inputs = "embeddings",
                             embed_dim = 64, num_heads = 4) |>
  scorch_transformer_encoder("enc2", inputs = "enc1",
                             embed_dim = 64, num_heads = 4)
}

}
\seealso{
Other model construction: 
\code{\link{compile_scorch}()},
\code{\link{initiate_scorch}()},
\code{\link{plot_scorch_model}()},
\code{\link{print.scorch_model}()},
\code{\link{scorch_add_skip}()},
\code{\link{scorch_attention}()},
\code{\link{scorch_batchnorm}()},
\code{\link{scorch_concat}()},
\code{\link{scorch_conv_transpose}()},
\code{\link{scorch_dropout}()},
\code{\link{scorch_flatten}()},
\code{\link{scorch_function}()},
\code{\link{scorch_input}()},
\code{\link{scorch_layer}()},
\code{\link{scorch_output}()},
\code{\link{scorch_pool}()}
}
\concept{model construction}
