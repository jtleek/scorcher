% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scorch_conv_transpose.R
\name{scorch_conv_transpose}
\alias{scorch_conv_transpose}
\title{Add a Transposed Convolution Node to a Scorch Model}
\usage{
scorch_conv_transpose(
  scorch_model,
  name,
  inputs = NULL,
  in_channels,
  out_channels,
  kernel_size,
  type = "2d",
  ...
)
}
\arguments{
\item{scorch_model}{A \code{scorch_model} object created by
\code{\link{initiate_scorch}}.}

\item{name}{A unique character string identifying this node in the
model graph. Names wire the computation graph -- other nodes
reference them via their \code{inputs} argument to define
branching, fusion, and skip connections. Names are arbitrary but
appear in error messages and \code{\link{plot_scorch_model}}
output. Common prefixes: \code{"fc"} (linear), \code{"conv"}
(convolution), \code{"act"} (activation). Use number suffixes
for multiples (e.g., \code{"fc1"}, \code{"fc2"}).}

\item{inputs}{Character vector of upstream node names. If \code{NULL}
(default), resolved automatically (last node or sole input).}

\item{in_channels}{Integer. Number of input channels.}

\item{out_channels}{Integer. Number of output channels.}

\item{kernel_size}{Integer or tuple. Size of the convolving kernel.}

\item{type}{Character. Dimensionality variant:
\code{"1d"}, \code{"2d"} (default), or \code{"3d"}.}

\item{...}{Additional arguments passed to the underlying
\code{torch::nn_conv_transpose*} function (e.g., \code{stride},
\code{padding}, \code{output_padding}).}
}
\value{
The updated \code{scorch_model} with a new row appended to
its \code{graph} tibble.
}
\description{
Convenience wrapper that adds a transposed convolution
(deconvolution) node to the Scorch model graph. Supports 1D, 2D,
and 3D variants. Commonly used in decoder pathways, U-Nets,
autoencoders, and generative models.
}
\details{
This is equivalent to calling
\code{scorch_layer(model, name, "conv_transpose2d", ...)} but
provides a more readable API with a \code{type} parameter for
selecting dimensionality.
}
\examples{
\dontrun{
# 2D transposed convolution for upsampling
model <- model |>
  scorch_conv_transpose("deconv1",
                        in_channels = 64, out_channels = 32,
                        kernel_size = 4, stride = 2, padding = 1)

# 1D transposed convolution for sequence generation
model <- model |>
  scorch_conv_transpose("deconv1", type = "1d",
                        in_channels = 128, out_channels = 64,
                        kernel_size = 3)
}

}
\seealso{
Other model construction: 
\code{\link{compile_scorch}()},
\code{\link{initiate_scorch}()},
\code{\link{plot_scorch_model}()},
\code{\link{print.scorch_model}()},
\code{\link{scorch_add_skip}()},
\code{\link{scorch_attention}()},
\code{\link{scorch_batchnorm}()},
\code{\link{scorch_concat}()},
\code{\link{scorch_dropout}()},
\code{\link{scorch_flatten}()},
\code{\link{scorch_function}()},
\code{\link{scorch_input}()},
\code{\link{scorch_layer}()},
\code{\link{scorch_output}()},
\code{\link{scorch_pool}()},
\code{\link{scorch_transformer_encoder}()}
}
\concept{model construction}
